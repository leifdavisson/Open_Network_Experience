version: '3.8'

services:
  # 1. Central Monitoring Platform (CMP) API Control Plane
  cmp-server:
    build:
      context: ../..
      dockerfile: server/Dockerfile
    image: open-ux/cmp-server:latest
    container_name: cmp-server
    restart: always
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DB_PATH=/app/data/cmp.db
      - BACKUP_DIR=/app/data/backups
    volumes:
      - cmp-data:/app/data

  # 2. VictoriaMetrics (Prometheus-compatible Time-Series Database)
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.101.0
    container_name: victoriametrics
    restart: always
    ports:
      - "8428:8428"
    volumes:
      - vm-data:/victoria-metrics-data
      - ./scrape.yml:/etc/prometheus/scrape.yml:ro
    command:
      - "-retentionPeriod=13" # In months, matching our retention standard
      - "-promscrape.config=/etc/prometheus/scrape.yml"
      - "-promscrape.config.strictParse=false"

  # 3. Grafana Loki (Log Ingestion & Logging Endpoint)
  loki:
    image: grafana/loki:3.0.0
    container_name: loki
    user: "root"
    restart: always
    ports:
      - "3100:3100"
    volumes:
      - ./loki-config.yaml:/etc/loki/local-config.yaml:ro
      - loki-data:/tmp/loki
    command: -config.file=/etc/loki/local-config.yaml

  # 5. Prometheus Alertmanager (Alerting Daemon)
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: alertmanager
    restart: always
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - am-data:/alertmanager
    command: --config.file=/etc/alertmanager/alertmanager.yml

  # 5. Grafana Analytics & Incident Dashboard (Default User Interface)
  grafana:
    image: grafana/grafana:11.0.0
    container_name: grafana
    restart: always
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin-noc-password-change-me
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SECURITY_ALLOW_EMBEDDING=true
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_ANONYMOUS_ORG_NAME=Main Org.
      - GF_SECURITY_COOKIE_SAMESITE=none
      - GF_SECURITY_COOKIE_SECURE=false
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-piechart-panel
    volumes:
      - ./grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
      - ./grafana-dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro
      - ./dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana

volumes:
  cmp-data:
  vm-data:
  loki-data:
  am-data:
  grafana-data:
